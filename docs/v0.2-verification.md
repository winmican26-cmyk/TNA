# TNA Gate v0.2 Verification

## Reproduction

Run from `C:\Users\mican\Documents\TNA` with Node.js 24+ and npm:

```powershell
npm ci --ignore-scripts
npm run typecheck
npm run lint
npm run build
node --test dist/tests/**/*.test.js
npm run demo:v02
```

The requested producer checks are also:

```powershell
npm run typecheck
npm run build
npm run demo:v02
npx eslint scripts/demo-v02.ts
```

The test glob includes the 53 permanent Vol 1 regression tests and 27 focused Vol 2 capability, broker, and API tests. `npm run demo:v02` rebuilds before running the compiled demo.

## Acceptance matrix

| Area | Acceptance evidence | Expected |
|---|---|---|
| Vol 1 regression | All existing `dist/tests/**/*.test.js` tests | 53 passed, 0 failed |
| Capability core | HMAC, canonicalization, malformed/forged/modified/expired/TTL cases | PASS |
| Broker unit tests | Registry privacy, issuance, binding, replay, revocation, stale policy, failure, concurrency, evidence | PASS |
| Vol 2 focused suite | Capability, broker, and `tests/integration/api-v02.test.ts` tests | 27 passed, 0 failed |
| Direct invocation | Demo POST to absent tool route | HTTP 404/rejected; no handler call |
| Approval gate | Demo authorization without approval | HOLD; no capability |
| Authorized execution | Demo approval, ALLOW, issue, redeem | CAPABILITY ISSUED and SUCCEEDED |
| Replay | Second redeem of same token | HTTP 409; no second invocation |
| Revocation | Issue a second capability, revoke, redeem | HTTP 403; BLOCKED transition |
| Evidence | Store hash-chain verification and cleanup | PASS; no secrets/output retained |

Observed full-suite result: `tests 80`, `pass 80`, `fail 0`, consisting of 53 Vol 1 regression tests and 27 focused Vol 2 tests.

## Evidence interpretation

A passing local run verifies the implemented code paths under Node.js and temporary local state. It does not certify production isolation, external side-effect atomicity, KMS/HSM key custody, or a compromised-host threat model.
